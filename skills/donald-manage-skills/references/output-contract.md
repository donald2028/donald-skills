# Output Contract

Report:

- the canonical skill files and repository metadata changed;
- the Skills CLI discovery result;
- the channel manifest build/check result;
- the available runtime-specific plugin validation results;
- the runtime mirror sync result;
- any check that was skipped and why;
- the commit, remote, and release result only when those actions were requested.
