interface:
  display_name: "Prepare Chrome Over CDP"
  short_description: "Set up or repair a reusable Chrome Profile binding"
  default_prompt: "Use $donald-config-browser for first-time setup or repair: choose a target skill, bind its Chrome Profile, reuse shared per-Profile login state, and verify agent-browser over CDP."
