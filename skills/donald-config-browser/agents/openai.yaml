interface:
  display_name: "Configure Donald Browser Binding"
  short_description: "Internal binding setup for Donald browser skills"
  default_prompt: "Use $donald-config-browser to configure, inspect, change, reset, or repair the Chrome Profile binding for a specific Donald browser skill."

policy:
  allow_implicit_invocation: false
